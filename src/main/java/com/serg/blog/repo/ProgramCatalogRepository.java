package com.serg.blog.repo;

import com.serg.blog.models.ProgramCatalog;
import org.springframework.data.repository.CrudRepository;

public interface ProgramCatalogRepository extends CrudRepository<ProgramCatalog, Long> {
}
